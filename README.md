# VLA Field Manual

A self-contained study manual on vision-language-action models.

The whole document is a single HTML file: equations, figures, code listings and
fonts are all embedded, so it renders with no network connection and no
JavaScript. The interactive layer (symbol tooltips, widgets, navigation) needs a
real browser engine.

Published at the GitHub Pages URL for this repository.
